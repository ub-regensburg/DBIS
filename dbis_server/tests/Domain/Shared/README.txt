In order to config the authentication test, you have to provide valid 
credentials.

Use .authtest-config.json, remove the . before the filename and insert 
the valid values in the json-File.

NEVER EVER remove .gitignore in this folder and/or push your credentials to git!